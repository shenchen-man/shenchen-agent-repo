# Test Web App

一个简单的web程序，页面显示"test"。

## 使用方法

### 方法1: 使用Node.js服务器（推荐）

1. 安装依赖：
```bash
npm install
```

2. 启动服务器：
```bash
npm start
```

3. 在浏览器中打开 http://localhost:3000

### 方法2: 直接打开HTML文件

直接双击 `index.html` 文件在浏览器中打开即可。